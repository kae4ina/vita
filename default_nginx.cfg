events {
    worker_connections 1024;
}
http {
  upstream app {
    server 127.0.0.1:80;
   
  }

  server {
	location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
    }

    listen 80;
    server_name localhost;

    
  }
  }
